{
    "scripts": {
        "test": "elm-format --validate src && elm-verify-examples --run-tests"
    },
    "dependencies": {
        "elm": "0.19.0-no-deps",
        "elm-format": "^0.8.2",
        "elm-verify-examples": "^4.0.0"
    }
}
